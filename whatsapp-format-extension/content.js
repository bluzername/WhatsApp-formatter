/**
 * WhatsApp Formatter - Content Script
 *
 * This is a minimal content script included for compatibility.
 * The main functionality is handled by the background service worker
 * using chrome.scripting.executeScript() for DOM access.
 *
 * This file can be extended in the future for additional features like:
 * - Keyboard shortcuts
 * - Visual feedback overlays
 * - Format preview tooltips
 *
 * @author WhatsApp Formatter Extension
 * @version 1.0.0
 */

// Content script is currently minimal - all logic is in background.js
// The service worker injects functions directly when needed
